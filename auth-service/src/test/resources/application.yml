spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/auth_db_grievance?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Nike2004$
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  cloud:
    config:
      enabled: false
  config:
    import:

eureka:
  client:
    enabled: false

jwt:
  secret: THIS_IS_A_TEMP_SECRET_CHANGE_IT_LATER_32CHARS
  expiration: 86400000