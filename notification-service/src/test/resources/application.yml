spring:
  application:
    name: notification-service
  datasource:
    url: jdbc:mysql://localhost:3306/feedback_db_grievance?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Nike2004$
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  cloud:
    config:
      enabled: false
  config:
    import:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  mail:
    host: localhost
    port: 1025

server:
  port: 8087

eureka:
  client:
    enabled: false
